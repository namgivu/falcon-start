[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
falcon = "==1.3.0"  # required fix version
#falcon = "*"  #TODO getting unittest error with version >1.3.0 > AttributeError: 'Test' object has no attribute '_default_headers' at tests.route.gc_intern_1910.test_health.Test.test()

gunicorn = "*"  # to run falcon api
httpie = "*"  # to make request from shell

# for testing
pytest = "*"
pytest-dotenv = "*"  # autoload .env when running test

# for postgres db
sqlalchemy = "*"
psycopg2 = "==2.7.7"

[requires]
python_version = "3.6"
